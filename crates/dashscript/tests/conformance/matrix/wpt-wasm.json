[
  {
    "id": "wpt.wasm.jsapi.constructor.compile",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Symbol` has no static mapping — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Promise` has no static mapping — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking)",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.constructor.instantiate",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new <non-identifier>(…)` has no static lowering → engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `Symbol` has no static mapping — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Promise` has no static mapping — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new <non-identifier>(…)` has no static lowering → engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.constructor.instantiate-bad-imports",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `Object.create` reflection is unsupported | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `Object.create` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `Object.create` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.constructor.multi-value",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.constructor.tostringtag",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `<builtin>.prototype.<method>` reflection needs the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `Object.defineProperty` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Object.defineProperty` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `<builtin>.prototype.<method>` reflection needs the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `Object.defineProperty` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `delete` has no DashScript mapping | `Symbol` has no static mapping — the function runs under the engine | `<builtin>.prototype.<method>` reflection needs the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.constructor.validate",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.exports.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`instanceof` has no DashScript mapping (static types; no runtime type check) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.global-exports-live-bindings.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.global-exports.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.js-wasm-cycle.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:10:10: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:7:17: warning: unreachable statement: unreachable statement\nsrc\\main.rs:7:50: error[E0425]: cannot find function `f` in this scope: not found in this scope\nerror: could not compile `probe` (bin \"probe\") due to 2 previous errors; 1 warning emitted",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.mutable-global-sharing.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:18:10: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:53:10: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:65:10: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:105:10: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:146:10: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:7:17: warning: unreachable statement: unreachable statement",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.namespace-instance.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation)",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.reserved-import-names.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:6:37: error[E0425]: cannot find value `t` in this scope: not found in this scope\nsrc\\main.rs:6:40: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:13:37: error[E0425]: cannot find value `t` in this scope: not found in this scope\nsrc\\main.rs:13:40: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:20:37: error[E0425]: cannot find value `t` in this scope: not found in this scope\nsrc\\main.rs:20:40: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.resolve-export.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:6:37: error[E0425]: cannot find value `t` in this scope: not found in this scope\nsrc\\main.rs:6:40: error[E0425]: cannot find value `syntax_error` in this scope: not found in this scope\nsrc\\main.rs:9:10: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:6:18: error[E0425]: cannot find function `promise_rejects_js` in this scope: not found in this scope\nsrc\\main.rs:6:18: warning: unreachable call: unreachable call\nerror: could not compile `probe` (bin \"probe\") due to 4 previous errors; 1 warning emitted",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.string-builtins.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:36:10: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:7:17: warning: unreachable statement: unreachable statement\nsrc\\main.rs:6:21: error[E0282]: type annotations needed\nerror: could not compile `probe` (bin \"probe\") due to 2 previous errors; 1 warning emitted",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.string-constants.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:22:10: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:7:17: warning: unreachable statement: unreachable statement\nsrc\\main.rs:6:21: error[E0282]: type annotations needed\nerror: could not compile `probe` (bin \"probe\") due to 2 previous errors; 1 warning emitted",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.v128-tdz.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:18:10: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:7:17: warning: unreachable statement: unreachable statement\nsrc\\main.rs:6:21: error[E0282]: type annotations needed\nerror: could not compile `probe` (bin \"probe\") due to 2 previous errors; 1 warning emitted",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.esm-integration.wasm-import-wasm-export.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`delete` has no DashScript mapping",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.exception.basic.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.exception.constructor.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `Symbol` has no static mapping — the function runs under the engine | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.exception.getarg.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`.constructor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isFrozen` (extensibility state) needs the engine (no static freeze tracking) | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.exception.identity.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.exception.is.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`.constructor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isFrozen` (extensibility state) needs the engine (no static freeze tracking) | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.exception.tostring.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.function.call.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.function.constructor.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `assert_implements` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `assert_implements` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `assert_implements` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `assert_implements` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `assert_implements` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `new <non-identifier>(…)` has no static lowering → engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `assert_implements` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `new <non-identifier>(…)` has no static lowering → engine | `assert_implements` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `new <non-identifier>(…)` has no static lowering → engine | `assert_implements` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `new <non-identifier>(…)` has no static lowering → engine | `assert_implements` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `new <non-identifier>(…)` has no static lowering → engine | `assert_implements` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `new <non-identifier>(…)` has no static lowering → engine | `assert_implements` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.function.table.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check)",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.function.type.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.gc.casts.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.gc.exported-object.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Object.defineProperty` reflection is unsupported | `Object.defineProperty` reflection is unsupported | `delete` has no DashScript mapping | `delete` has no DashScript mapping | `delete` has no DashScript mapping | `delete` has no DashScript mapping | `delete` has no DashScript mapping | `delete` has no DashScript mapping | `delete` has no DashScript mapping | `delete` has no DashScript mapping | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.preventExtensions` (extensibility state) needs the engine (no static freeze tracking) | `Object.preventExtensions` (extensibility state) needs the engine (no static freeze tracking) | `Object.seal` (extensibility state) needs the engine (no static freeze tracking) | `Object.seal` (extensibility state) needs the engine (no static freeze tracking) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `<builtin>.prototype.<method>` reflection needs the engine | `<builtin>.prototype.<method>` reflection needs the engine | `<builtin>.prototype.<method>` reflection needs the engine | `<builtin>.prototype.<method>` reflection needs the engine | `WeakMap` has no static mapping — the function runs under the engine | `WeakSet` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.gc.i31.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.global.constructor",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | object accessor properties (get/set) are unsupported | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.global.tostring",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`new <non-identifier>(…)` has no static lowering → engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.global.type.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.global.value-get-set",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Object.create` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.global.valueof",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.idlharness",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`.constructor` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Proxy` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Set` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Object.getPrototypeOf` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `isPrototypeOf` (prototype reflection) is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `Object.isFrozen` (extensibility state) needs the engine (no static freeze tracking) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Object` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `isConstructor` is a test262 harness helper — runs under the engine | `isConstructor` is a test262 harness helper — runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `.constructor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertySymbols` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `Reflect.<method>` has no static mapping — runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `delete` has no DashScript mapping | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `eval` is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Object` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_inherits` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_inherits` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `.constructor` reflection is unsupported | `.constructor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Object` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.instance.constructor",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new <non-identifier>(…)` has no static lowering → engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.instance.constructor-bad-imports",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `Object.create` reflection is unsupported | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `Object.create` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `Object.create` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.instance.constructor-caching",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.instance.exports",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.instance.tostring",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.interface",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.js-string.basic",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `BigInt` has no static mapping — the function runs under the engine | `BigInt` has no static mapping — the function runs under the engine | `BigInt` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` has no static mapping — the function runs under the engine | `BigInt` has no static mapping — the function runs under the engine | `BigInt` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `instanceof` has no DashScript mapping (static types; no runtime type check) | `BigInt` literals are unsupported | `new Number()` has no static mapping — runs under the engine | `new Boolean()` has no static mapping — runs under the engine | `Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.js-string.constants",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.js-string.imports",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.jspi.js-promise-integration",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Promise` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Promise` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Promise` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Proxy` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Promise.<method>` has no static mapping — runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Promise` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Promise` has no static mapping — the function runs under the engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.jspi.notraps",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.jspi.rejects",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Promise` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.memory.buffer",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.memory.constructor",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isFrozen` (extensibility state) needs the engine (no static freeze tracking) | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Proxy` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | object accessor properties (get/set) are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.memory.constructor-shared.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isFrozen` (extensibility state) needs the engine (no static freeze tracking) | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | object accessor properties (get/set) are unsupported | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.memory.constructor-types.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isFrozen` (extensibility state) needs the engine (no static freeze tracking) | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.memory.grow",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`.constructor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isFrozen` (extensibility state) needs the engine (no static freeze tracking) | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.memory.to-fixed-length-buffer",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.memory.to-fixed-length-buffer-shared",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.memory.to-resizable-buffer",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.memory.to-resizable-buffer-shared",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.memory.tostring",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`new <non-identifier>(…)` has no static lowering → engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.memory.type.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.module.constructor",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Object.getPrototypeOf` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `Object.getPrototypeOf` reflection is unsupported",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.module.customsections",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | a string literal with a lone surrogate needs the engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.module.exports",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.module.imports",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.module.modulesource.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.module.tostring",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.prototypes",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `new <non-identifier>(…)` has no static lowering → engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check)",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.table.constructor",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Proxy` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | object accessor properties (get/set) are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.table.constructor-types.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.table.get-set",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.table.grow",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.table.length",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.table.tostring",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`new <non-identifier>(…)` has no static lowering → engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.table.type.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.tag.constructor.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.tag.tostring.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`new <non-identifier>(…)` has no static lowering → engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.jsapi.tag.type.tentative",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.serialization.memory.clone-error.tentative.https",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.serialization.module.nested-worker-success",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`new <non-identifier>(…)` has no static lowering → engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Promise` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.serialization.module.serialization-via-idb",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`new Date()` has no static mapping — runs under the engine | `Date` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.defineProperty` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `async_test` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `new <non-identifier>(…)` has no static lowering → engine | `async_test` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `async_test` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported",
    "note": ""
  },
  {
    "id": "wpt.wasm.serialization.module.serialization-via-notifications-api",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`new <non-identifier>(…)` has no static lowering → engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.abort",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:21:21: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:40:31: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:66:29: error[E0425]: cannot find value `promise` in this scope: not found in this scope\nsrc\\main.rs:69:25: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:9:34: error[E0433]: cannot find type `AbortController` in this scope: use of undeclared type `AbortController`\nsrc\\main.rs:22:78: error[E0277]: the trait bound `&std::string::String: DsDisplay` is not satisfied: the trait `DsDisplay` is not implemented for `&std::string::String`",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.body",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.contenttype",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.empty-body",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:18:25: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:19:25: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:24:22: error[E0425]: cannot find value `name` in this scope: not found in this scope\nsrc\\main.rs:32:25: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:33:25: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:38:40: error[E0425]: cannot find value `name` in this scope: not found in this scope",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.historical",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:8:38: error[E0425]: cannot find value `indexed_db` in this scope: not found in this scope\nsrc\\main.rs:14:36: error[E0425]: cannot find value `indexed_db` in this scope: not found in this scope\nsrc\\main.rs:24:22: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:12:14: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:23:14: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks\nsrc\\main.rs:26:14: error[E0728]: `await` is only allowed inside `async` functions and blocks: only allowed inside `async` functions and blocks",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.idlharness",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`.constructor` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Proxy` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Set` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Object.getPrototypeOf` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `isPrototypeOf` (prototype reflection) is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `Object.isFrozen` (extensibility state) needs the engine (no static freeze tracking) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Object` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `isConstructor` is a test262 harness helper — runs under the engine | `isConstructor` is a test262 harness helper — runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `.constructor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertySymbols` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `Reflect.<method>` has no static mapping — runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `delete` has no DashScript mapping | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `eval` is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Object` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_inherits` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_inherits` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `.constructor` reflection is unsupported | `.constructor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — WinterTC is static-only, no degradation | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `Object` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering)",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.instantiatestreaming",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new <non-identifier>(…)` has no static lowering → engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | `BigInt` literals are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.instantiatestreaming-bad-imports",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `Object.create` reflection is unsupported | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `BigInt` literals are unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `Object.create` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `Symbol` has no static mapping — the function runs under the engine | `Object.create` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.invalid-args",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Symbol` has no static mapping — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.invalid-code",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.modified-contenttype",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`DataView` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.origin.sub",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:16:21: error[E0425]: cannot find value `type_error` in this scope: not found in this scope\nsrc\\main.rs:17:21: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:34:21: error[E0425]: cannot find value `type_error` in this scope: not found in this scope\nsrc\\main.rs:35:21: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:14:17: error[E0425]: cannot find function `promise_rejects_js` in this scope: not found in this scope\nsrc\\main.rs:20:73: error[E0277]: the trait bound `&std::string::String: DsDisplay` is not satisfied: the trait `DsDisplay` is not implemented for `&std::string::String`",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.rejected-arg",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`Promise.<method>` has no static mapping — runs under the engine",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.status",
    "layer": "wpt",
    "category": "wasm",
    "status": "partial",
    "detail": "static build: src\\main.rs:17:25: error[E0425]: cannot find value `type_error` in this scope: not found in this scope\nsrc\\main.rs:18:25: error[E0425]: cannot find value `web_assembly` in this scope: not found in this scope\nsrc\\main.rs:12:74: error[E0277]: the trait bound `f64: DsDisplay` is not satisfied: the trait `DsDisplay` is not implemented for `f64`\nsrc\\main.rs:15:21: error[E0425]: cannot find function `promise_rejects_js` in this scope: not found in this scope\nsrc\\main.rs:23:77: error[E0277]: the trait bound `&std::string::String: DsDisplay` is not satisfied: the trait `DsDisplay` is not implemented for `&std::string::String`\nsrc\\main.rs:8:13: error[E0425]: cannot find function `promise_test` in this scope: not found in this scope",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.wasm_stream_compile_test",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`instanceof` has no DashScript mapping (static types; no runtime type check) | `new <non-identifier>(…)` has no static lowering → engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `new <non-identifier>(…)` has no static lowering → engine | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check)",
    "note": ""
  },
  {
    "id": "wpt.wasm.webapi.wasm_stream_instantiate_test",
    "layer": "wpt",
    "category": "wasm",
    "status": "unsupported",
    "detail": "`instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check) | `instanceof` has no DashScript mapping (static types; no runtime type check)",
    "note": ""
  }
]
