[
  {
    "id": "wpt.webcryptoapi.crypto_key_cached_slots.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "partial",
    "detail": "static build: src\\main.rs:14:26: error[E0599]: no method named `then` found for opaque type `impl std::future::Future<Output = DsCryptoKey>` in the current scope: method not found in `impl std::future::Future<Output = DsCryptoKey>`\nsrc\\main.rs:40:26: error[E0599]: no method named `then` found for opaque type `impl std::future::Future<Output = DsCryptoKey>` in the current scope: method not found in `impl std::future::Future<Output = DsCryptoKey>`\nsrc\\__ds.rs:660:9: warning: unused import: `::hmac::Mac`\nsrc\\__ds.rs:694:9: warning: unused import: `::hmac::Mac`\nsrc\\__ds.rs:783:9: warning: unused import: `::aead::Aead`\nsrc\\__ds.rs:797:9: warning: unused import: `::aead::Aead` | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.derive_bits_keys.argon2.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.derive_bits_keys.cfrg_curves_bits_curve25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.derive_bits_keys.cfrg_curves_bits_curve448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.derive_bits_keys.cfrg_curves_keys_curve25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.derive_bits_keys.cfrg_curves_keys_curve448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.derive_bits_keys.derive_key_and_encrypt.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.derive_bits_keys.derived_bits_length.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a local of unknown type — engine runs the real check | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.derive_bits_keys.ecdh_bits.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.derive_bits_keys.ecdh_keys.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.derive_bits_keys.hkdf.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.derive_bits_keys.pbkdf2.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.digest.cshake.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.digest.digest.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.digest.kangarootwelve.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.digest.sha3.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.digest.turboshake.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.encap_decap.encap_decap_bits.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a local of unknown type — engine runs the real check | `hasOwnProperty` (prototype reflection) is unsupported | `hasOwnProperty` (prototype reflection) is unsupported | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.encap_decap.encap_decap_keys.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a local of unknown type — engine runs the real check | `hasOwnProperty` (prototype reflection) is unsupported | `hasOwnProperty` (prototype reflection) is unsupported | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.encrypt_decrypt.aes_cbc.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.encrypt_decrypt.aes_ctr.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.encrypt_decrypt.aes_gcm.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.encrypt_decrypt.aes_gcm_256_iv.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.encrypt_decrypt.aes_ocb.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.encrypt_decrypt.chacha20_poly1305.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.encrypt_decrypt.rsa_oaep.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_aes-cbc.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_aes-ctr.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_aes-gcm.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_aes-kw.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_aes-ocb.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_chacha20_poly1305.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_ecdh.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_ecdsa.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_ed25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_ed448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_hmac.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_kmac.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_ml-dsa.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_ml-kem.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_rsa-oaep.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_rsa-pss.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_rsassa-pkcs1-v1_5.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_x25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.failures_x448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_aes-cbc.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_aes-ctr.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_aes-gcm.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_aes-kw.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_aes-ocb.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_chacha20_poly1305.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_ecdh.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_ecdsa.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_ed25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_ed448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_hmac.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_kmac.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_ml-dsa.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_ml-kem.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_rsa-oaep.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_rsa-pss.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_rsassa-pkcs1-v1_5.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_x25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.generatekey.successes_x448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | optional chaining beyond a single `?.field` (a `?.method()` call, `?.[k]`, or a nested `a?.b?.c`) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: location is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.getpublickey.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine error: AssertionError: getPublicKey method is available: harness threw ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.getrandomvalues",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `new DataView()` has no static mapping — runs under the engine | `new ArrayBuffer()` has no static mapping — runs under the engine | `new DataView()` has no static mapping — runs under the engine | `new ArrayBuffer()` has no static mapping — runs under the engine | `.constructor` reflection is unsupported | engine: engine error: AssertionError: Float16Array DOMException name mismatch: TypeError: cannot read property 'getRandomValues' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.historical",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "partial",
    "detail": "static build: src\\main.rs:5:15: error[E0425]: cannot find value `self_` in this scope: not found in this scope\nsrc\\main.rs:8:42: error[E0425]: cannot find value `self_` in this scope: not found in this scope\nsrc\\main.rs:12:15: error[E0425]: cannot find value `self_` in this scope: not found in this scope\nsrc\\main.rs:15:42: error[E0425]: cannot find value `self_` in this scope: not found in this scope\nsrc\\main.rs:19:15: error[E0425]: cannot find value `self_` in this scope: not found in this scope\nsrc\\main.rs:22:42: error[E0425]: cannot find value `self_` in this scope: not found in this scope | engine: engine error: AssertionError: Non-secure context window does not have access to crypto.subtle: harness threw TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.idlharness.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `.constructor` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new Proxy()` has no static mapping — runs under the engine | `new Proxy()` has no static mapping — runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `Object.getPrototypeOf` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `isPrototypeOf` (prototype reflection) is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `Object.isFrozen` (extensibility state) needs the engine (no static freeze tracking) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `isConstructor` is a test262 harness helper — runs under the engine | `isConstructor` is a test262 harness helper — runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `.constructor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertySymbols` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `Reflect.<method>` has no static mapping — runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `delete` has no DashScript mapping | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `eval` is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_inherits` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_inherits` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `.constructor` reflection is unsupported | `.constructor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | engine: engine lacks built-in: ReferenceError: fetch is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.idlharness.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `.constructor` reflection is unsupported | `new <non-identifier>(…)` has no static lowering → engine | `new Proxy()` has no static mapping — runs under the engine | `new Proxy()` has no static mapping — runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `Object.getPrototypeOf` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `isPrototypeOf` (prototype reflection) is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `Object.isFrozen` (extensibility state) needs the engine (no static freeze tracking) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a non-identifier operand — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `isConstructor` is a test262 harness helper — runs under the engine | `isConstructor` is a test262 harness helper — runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Function` as a value is unsupported (use it only as a static-call/new receiver or type annotation) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `.constructor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertySymbols` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.create` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `Reflect.<method>` has no static mapping — runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Object.setPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `delete` has no DashScript mapping | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `Symbol` has no static mapping — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `eval` is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_inherits` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `assert_inherits` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `.constructor` reflection is unsupported | `.constructor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.create` reflection is unsupported | `prototype` mutation is unsupported | `Object.getOwnPropertyDescriptor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `assert_own_property` is a WPT testharness function with no static lowering (async/composite) — the fixture falls back to the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.isExtensible` (extensibility state) needs the engine (no static freeze tracking) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `instanceof` on a local of unknown type — engine runs the real check | `this` outside a class method needs the engine (no static lowering) | `Object.getPrototypeOf` reflection is unsupported | `Reflect.<method>` has no static mapping — runs under the engine | `Object.getPrototypeOf` reflection is unsupported | `Object.getPrototypeOf` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `Object.getOwnPropertyDescriptor` reflection is unsupported | `this` outside a class method needs the engine (no static lowering) | `prototype` mutation is unsupported | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | `this` outside a class method needs the engine (no static lowering) | engine: engine lacks built-in: ReferenceError: fetch is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.aes-ocb_importkey.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | object accessor properties (get/set) are unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.argon2_importkey.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | object accessor properties (get/set) are unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.chacha20-poly1305_importkey.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | object accessor properties (get/set) are unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.crashtests.importkey-unsettled-promise.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.ec_importkey.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `.constructor` reflection is unsupported | `.constructor` reflection is unsupported | `instanceof` on a local of unknown type — engine runs the real check | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.ec_importkey_failures_ecdh.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.ec_importkey_failures_ecdsa.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.kmac_importkey.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | object accessor properties (get/set) are unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.ml-dsa_importkey.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.ml-kem_importkey.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.okp_importkey_ed25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `.constructor` reflection is unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.okp_importkey_ed448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `.constructor` reflection is unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.okp_importkey_failures_ed25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.okp_importkey_failures_ed448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.okp_importkey_failures_x25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.okp_importkey_failures_x448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.okp_importkey_x25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `.constructor` reflection is unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.okp_importkey_x448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `.constructor` reflection is unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.raw_format_aliases.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.rsa_importkey.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.import_export.symmetric_importkey.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | object accessor properties (get/set) are unsupported | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.normalize-algorithm-name.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static build",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.randomuuid.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "partial",
    "detail": "static build: src\\main.rs:7:40: error[E0599]: no method named `has` found for struct `HashSet<T, S, A>` in the current scope\nsrc\\main.rs:19:15: error[E0599]: no method named `add` found for struct `HashSet<T, S, A>` in the current scope: method not found in `HashSet<_>`\nsrc\\main.rs:20:9: error[E0308]: mismatched types: expected `f64`, found `String`\nsrc\\main.rs:29:32: error[E0308]: mismatched types: expected `f64`, found `i64`\nsrc\\main.rs:31:53: error[E0599]: no method named `as_str` found for type `f64` in the current scope: method not found in `f64`\nsrc\\main.rs:41:32: error[E0308]: mismatched types: expected `f64`, found `i64` | engine: engine error: AssertionError: namespace format: harness threw TypeError: cannot read property 'randomUUID' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.aes-cbc.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.aes-ctr.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.aes-gcm.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.aes-kw.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.aes-ocb.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.chacha20-poly1305.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.ecdh.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.ecdsa.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.ed25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.ed448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.hmac.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.kmac.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.mldsa.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.mlkem.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.rsa-oaep.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.rsa-pss.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.rsassa-pkcs1-v1_5.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.x25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.serialization.x448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "supported",
    "detail": "engine fallback after static check",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.sign_verify.ecdsa.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: not a function",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.sign_verify.eddsa_curve25519.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.sign_verify.eddsa_curve448.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.sign_verify.eddsa_small_order_points.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.sign_verify.hmac.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: not a function",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.sign_verify.kmac.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: not a function",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.sign_verify.mldsa.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: not a function",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.sign_verify.rsa_pkcs.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: not a function",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.sign_verify.rsa_pss.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | object accessor properties (get/set) are unsupported | `new Promise()` has no static mapping — runs under the engine | `new Promise()` has no static mapping — runs under the engine | engine: engine error: TypeError: not a function",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.supports-modern.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a non-identifier operand — engine runs the real check | engine: engine error: AssertionError: SubtleCrypto.supports method exists: harness threw ReferenceError: SubtleCrypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.supports.tentative.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `instanceof` on a non-identifier operand — engine runs the real check | engine: engine lacks built-in: ReferenceError: crypto is not defined",
    "note": ""
  },
  {
    "id": "wpt.webcryptoapi.wrapkey_unwrapkey.wrapkey_unwrapkey.https",
    "layer": "wpt",
    "category": "webcryptoapi",
    "status": "out-of-scope",
    "detail": "static check: `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `.constructor` reflection is unsupported | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Symbol` has no static mapping — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `typeof` of a runtime value (a user identifier, member access, or call) has no static lowering — the function runs under the engine | `Promise.<method>` has no static mapping — runs under the engine | `Promise.<method>` has no static mapping — runs under the engine | `delete` has no DashScript mapping | `delete` has no DashScript mapping | `delete` has no DashScript mapping | `Promise.<method>` has no static mapping — runs under the engine | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `instanceof` on a local of unknown type — engine runs the real check | `Object.create` reflection is unsupported | `delete` has no DashScript mapping | `delete` has no DashScript mapping | `delete` has no DashScript mapping | `new DataView()` has no static mapping — runs under the engine | engine: engine error: TypeError: cannot read property 'subtle' of undefined",
    "note": ""
  }
]
